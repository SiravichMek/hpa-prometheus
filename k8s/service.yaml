apiVersion: v1
kind: Service
metadata:
  name: demo-app
  labels:
    app: demo-app
spec:
  type: NodePort
  selector:
    app: demo-app
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
    name: http

# Made with Bob
